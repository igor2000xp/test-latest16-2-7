// import { ComponentFixture, TestBed } from '@angular/core/testing';

// // import { initialState } from '../../../../store/actions/vehicle-state.actions';
// import { VehicleAutoComponent } from './vehicle-auto.component';
// import { ActionsSubject, ReducerManager, ReducerManagerDispatcher, StateObservable, Store } from '@ngrx/store';

describe('VehicleAutoComponent', () => {
  it('some test', () => {
    expect('').toEqual('');
  });
  //   let component: VehicleAutoComponent;
  //   let fixture: ComponentFixture<VehicleAutoComponent>;

  //   beforeEach(() => {
  //     TestBed.configureTestingModule({
  //       declarations: [VehicleAutoComponent,],
  //       providers: [Store, StateObservable, ActionsSubject, ReducerManager, ReducerManagerDispatcher,],
  //     });
  //     fixture = TestBed.createComponent(VehicleAutoComponent);
  //     component = fixture.componentInstance;
  //     fixture.detectChanges();
  //   });

  //   it('should create', () => {
  //     expect(component).toBeTruthy();
  //   });
  // });

  // // Jest

  // // import { VehicleAutoComponent } from './vehicle-auto.component';
  // // import { Store } from '@ngrx/store';
  // // import { of } from 'rxjs';

  // // describe('VehicleAutoComponent', () => {
  // //   let component: VehicleAutoComponent;
  // //   let store: Store;

  // //   beforeEach(() => {
  // //     store = {
  // //       dispatch: jest.fn(),
  // //       select: jest.fn(),
  // //       pipe: jest.fn(() => of())
  // //     } as any;

  // //     component = new VehicleAutoComponent(store);
  // //   });

  // //   it('should create', () => {
  // //     expect(component).toBeTruthy();
  // //   });

  // //   it('should add autoSubtype control on ngOnInit', () => {
  // //     component.ngOnInit();
  // //     expect(component.autoForm.contains('autoSubtype')).toBeTruthy();
  // //   });

  // //   it('should dispatch an action on change', () => {
  // //     const value = 'test';
  // //     component.onChange(value);
  // //     expect(store.dispatch).toHaveBeenCalledWith(
  // //       FormState.currentVehicleSubtype({ currentVehicleSubtype: value })
  // //     );
  // //   });
});
